name: Stale Dependencies

on:
  schedule:
    - cron: "30 5 * * 2"
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  stale-dependencies:
    name: Stale direct dependencies
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Rust maintenance tools
        uses: ./.github/actions/setup-rust-maintenance-tools
        with:
          install-stable-toolchain: "true"

      - name: Check stale direct dependencies
        shell: bash
        run: |
          set -euo pipefail

          report_file="$(mktemp)"
          status=0
          cargo +stable outdated --workspace --root-deps-only --exit-code 1 | tee "$report_file" || status=$?

          {
            echo "## Stale direct dependency report"
            echo
            echo '```text'
            cat "$report_file"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

          if [[ "$status" -eq 0 ]]; then
            exit 0
          fi

          if [[ "$status" -eq 1 ]]; then
            echo "Outdated direct workspace dependencies detected. Review the report above."
            exit 1
          fi

          exit "$status"
