# Design docs

Use this folder for short-lived design thinking, context, and rationale while a change is being shaped.

Design docs are temporary artifacts. Once a decision is stable, move the enduring parts into `docs/developer` or capture the actual decision in `docs/adr`.
